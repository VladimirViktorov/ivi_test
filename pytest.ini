[pytest]
markers =
    smoke: marker for smoke tests
    regression: marker for regression tests
    need_review: marker for tests that need a review

addopts =
    --reruns=2
    --alluredir=test_results